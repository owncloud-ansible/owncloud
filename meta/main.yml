# Standards: 0.1
---
galaxy_info:
  # @meta name:value: owncloud
  author: Robert Kaussow
  # @meta description: value: >
  # > **WARNING**: This Ansible role is currently in beta state. Use it at your own risk.
  #
  # Ansible role to setup an ownCloud server. Currently, only a single ownCloud server is supported.
  # Support for clustered mode is planned but not included right now.
  # @end
  description: Ansible role to setup an ownCloud server
  license: Apache-2.0
  min_ansible_version: 2.8
  platforms:
    - name: Ubuntu
      versions:
        - xenial
    - name: EL
      versions:
        - 7
  galaxy_tags:
    - owncloud
    - storage
    - sharing

dependencies: []

...
